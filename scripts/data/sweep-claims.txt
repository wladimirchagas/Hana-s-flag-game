# Flag-meaning subdivision sweep — shared claim ledger
#
# Purpose: two (or more) agents are expanding src/data/flagMeanings.ts in parallel.
# Both edit the SAME file, so before starting a country you MUST claim it here to
# avoid duplicated work and merge conflicts.
#
# Protocol:
#   1. `git fetch origin main` and read this file.
#   2. Pick a country NOT already listed as `done`, `omitted`, or `in-progress`.
#   3. Add/append a line here, commit THIS file first, push, and merge it before
#      you start sourcing that country.
#   4. Update your line to `done` / `omitted` when finished.
#   If check-flag-meanings.mjs reports a code you didn't add, someone else did —
#   fetch/rebase and drop your duplicate.
#
# Format:  <ISO alpha-2>  <agent>  <status>  # note
#   status = in-progress | done | omitted(=investigated, no sourceable symbolism) | partial
#
# Find remaining work:  node scripts/subdiv-remaining.mjs [CC]
# Log genuine omissions in: scripts/data/subdiv-meaning-omitted.txt
# ---------------------------------------------------------------------------
#
# ===== 2026-07-04 AUDIT — READ BEFORE OMITTING ANY SET (Antigravity: this is new scope) =====
# A pervasive wrong-omission bug was found: sets were batch-omitted as "image-only /
# unsourceable" after checking ONLY the subdivision's MAIN article or en.wikipedia. In fact
# their symbolism IS sourceable via a DEDICATED local-language arms/flag article and/or the
# Wikimedia Commons file blazon. Confirmed wrongly-omitted and now being RE-SOURCED:
#   IT (81 provinces — it.wiki Stemma + Commons blason), LT (10 counties — "X apskrities herbas"),
#   EE ("X maakonna vapp"), LV ("X ģerbonis"), MK ("Грб на X"), AR/CO ("Bandera de la provincia de X").
# NEW HARD RULE (CLAUDE.md rule 3a): before omitting a set you MUST exhaust all three of:
#   (a) the subdivision's own LOCAL-language wiki heraldry section (Stemma/Wappen/Vaakuna/Герб/…),
#   (b) the DEDICATED arms/flag article (main article often shows only the image), and
#   (c) the Wikimedia Commons file page (its description usually carries the full blazon).
# Only a coat of arms with NO textual blazon anywhere (e.g. RO counties — gazette-only) is a real wall.
# Antigravity: do NOT trust an old `omitted`/`wall` line without re-checking via (a)+(b)+(c).
# ---------------------------------------------------------------------------

# ===== COMPLETED (every displayed subdivision sourced, or investigated-and-omitted) =====
TH  claude  done       # Thailand — 77 provinces (provincial seals)
ID  claude  done       # Indonesia — all 33 provinces (id.wikipedia "Lambang X")
HU  claude  done       # Hungary — counties + cities (hu.wikipedia címer); 7 undocumented cities omitted
SE  claude  done       # Sweden — 21 counties (en.wikipedia province arms)
AT  claude  done       # Austria — 9 states (de.wikipedia "Wappen …")
FI  claude  done       # Finland — 18 regions (fi.wikipedia "…n vaakuna")
NO  claude  done       # Norway — remaining counties (en.wikipedia)
BY  claude  done       # Belarus — 7 regions (ru.wikipedia "Герб … области")
LR  claude  partial    # Liberia — Montserrado sourced; other 11 counties omitted (image-only)
AM  claude  done       # Armenia — Yerevan sourced (only remaining AM code)

# ===== CONFIRMED WALLS — image-only on reachable Wikipedias, no per-element symbolism =====
# Do NOT re-probe these via Wikipedia. Only attempt with a NON-Wikipedia authoritative
# source (government gazette / official decree). Otherwise leave for omission-logging.
RO  claude  wall       # Romania — county arms image-only (en + ro.wikipedia)
EG  claude  wall       # Egypt — governorate emblems image-only (ar.wikipedia)
PA  claude  wall       # Panama — province flags image-only (es.wikipedia)
SI  claude  wall       # Slovenia — municipal arms image-only (sl.wikipedia)
SK  claude  wall       # Slovakia — region arms composite/undescribed (sk.wikipedia)
MN  claude  wall       # Mongolia — aimag emblems image-only
AD  claude  wall       # Andorra — parish arms image-only (en + ca.wikipedia)
KG  claude  wall       # Kyrgyzstan — oblast emblems image-only (ru.wikipedia)
UY  claude  wall       # Uruguay — department flags image-only (es.wikipedia)
CL  claude  wall       # Chile — regional flags image-only / no official symbolism
MM  claude  wall       # Myanmar — state/region flags image-only (en.wikipedia)
ET  claude  wall       # Ethiopia — regional flags image-only (en.wikipedia)
GE  claude  wall       # Georgia — Tbilisi arms not described (en.wikipedia)
AZ  claude  wall       # Azerbaijan — Baku arms image-only (en.wikipedia)
PW  claude  wall       # Palau — state flags image-only (en.wikipedia)

# ===== IN PROGRESS =====
# (add your claim lines below before starting a country)
IT  claude  in-progress  # Italy — re-sourcing 81 provinces wrongly omitted by #401 (it.wiki Stemma); ~22/81 done, 59 left
LT  claude  done         # Lithuania — all 10 counties re-sourced ("X apskrities herbas"); was wrongly omitted
EE  claude  done         # Estonia — all counties re-sourced ("X maakonna vapp"); was wrongly omitted
LV  claude  in-progress  # Latvia — re-sourcing "X ģerbonis"; 31 left (was wrongly omitted)
CO  claude  done         # Colombia — 10 depts re-sourced ("Bandera de X"); CO-ATL/CAU/SAP omitted (no per-element symbolism, rule 3a exhausted)
AR  claude  done         # Argentina — all provinces re-sourced ("Bandera de la provincia de X"); was wrongly omitted
MK  claude  in-progress  # Macedonia — re-sourcing "Грб на X" (was wrongly omitted)
PE  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
PG  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
TW  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
PY  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
NI  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
SD  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
SV  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
AL  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
DO  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
SS  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
ZM  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
GA  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
HN  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
SO  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
VU  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
BH  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
CV  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
GH  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
CU  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
WF  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
AG  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
BW  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
DJ  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
GM  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
GW  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
MZ  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
ST  claude  omitted      # image-only wall — all codes logged omitted (rule 6)
BA  claude  omitted      # Brčko District flag image-only — omitted (rule 6)
NG  claude  wall         # Nigeria — state flags image-only (en.wikipedia)
ME  claude  wall         # Montenegro — municipal arms image-only (en + sr.wikipedia)
FR  claude  done         # France — all 97 departments + overseas collectivities sourced (Armorial/Commons)
MU  claude  wall         # Mauritius — Rodrigues/Port Louis flags image-only (en.wikipedia)
TT  claude  wall         # Trinidad & Tobago — regional corp flags image-only (en.wikipedia)
GL  claude  wall         # Greenland — municipal arms image-only (en.wikipedia)
UZ  claude  partial      # Uzbekistan — Karakalpakstan sourced; Tashkent omitted (image-only)
SB  claude  wall         # Solomon Islands — provincial flags image-only (en.wikipedia)
CR  claude  wall         # Costa Rica — province flags image-only (es.wikipedia)
CN  claude  done         # China — Hong Kong + Macau SAR flags sourced (en.wikipedia)
DK  claude  omitted      # Denmark — 5 regions use modern logos, no symbolism (omitted)
AE  claude  done         # UAE — 7 emirate flags sourced (en.wikipedia Flag of X / Trucial States)
SM  claude  done         # San Marino — 9 castelli sourced (it.wikipedia Stemma + national arms)
LI  claude  done         # Liechtenstein — 8 remaining communes sourced (de.wikipedia Wappen)
ZA  claude  partial      # South Africa — Western Cape sourced; other 6 provinces omitted (arms image-only)
GB  antigravity  done     # Great Britain — GB-NIR (Northern Ireland) sourced (en.wikipedia Ulster Banner)
CA  antigravity  done     # Canada — CA-PE (Prince Edward Island) sourced (en.wikipedia)
PK  antigravity  done     # Pakistan — 4 provinces sourced (en.wikipedia Flag of X articles)

# ===== SWEEP RE-OPENED (2026-07-04) — remaining:110 across 5 countries =====
# The audit above reversed the wrong omissions; those sets are now being re-sourced.
# Current: IT:59 LV:31 CO:13 AR:4 EE:3 (run `node scripts/subdiv-remaining.mjs` for live count).
# Antigravity: safe to claim LV or CO (independent of the Italy/Estonia work in flight) — add an
# `in-progress` line first, or leave them to claude. Do not re-omit without the (a)+(b)+(c) checks.
